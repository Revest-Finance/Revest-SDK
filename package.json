{
  "name": "revest-sdk",
  "version": "1.0.0",
  "description": "1. cd into hardhat 2. Run npm install 3. Create a .env file under /hardhat according to .envtemplate 4. Use \"npx hardhat run scripts/deploy-uniswap-test.js --network YOURNETWORKHERE\" to deploy the contract 5. Use 'npx hardhat run scripts/verify-uniswap-test.js --network YOURNETWORKHERE\" to verify the contract after pasting its address in",
  "main": "RevestSDK.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Revest-Finance/Revest-SDK.git"
  },
  "author": "RobAnon",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Revest-Finance/Revest-SDK/issues"
  },
  "homepage": "https://github.com/Revest-Finance/Revest-SDK#readme",
  "dependencies": {
    "ethers": "^5.5.4",
    "json-to-graphql-query": "^2.2.2"
  }
}
